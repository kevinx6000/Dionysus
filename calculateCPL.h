#ifndef CALCULATE_CPL_H
#define CALCULATE_CPL_H

#include "header.h"
#include "structure.h"

// Calculate CPL
bool calculateCPL(DPGraph &dpGraph, int CPL[]){

	// Start from indegree = 0
	for(int i = 0; i < dpGraph.nodes.size(); i++){
		if(!dpGraph.nodes[i].parent.size()){
		}
	}
}

#endif
